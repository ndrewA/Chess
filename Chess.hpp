
#ifndef CHESS_H
#define CHESS_H

#pragma once

#include <stdint.h>

enum class pieceType { king, queen, bishop, knight, rook, pawn, none };
enum class pieceColor { white, black, none };

#endif